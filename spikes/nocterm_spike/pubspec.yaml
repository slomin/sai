name: nocterm_spike
description: Throwaway spike evaluating nocterm for the sai terminal client (#10). Not shipped.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.5.0

dependencies:
  nocterm: ^0.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
