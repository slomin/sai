name: sai_core
description: Core of sai — task model, archive, provider routing and the shared riverpod layer. Pure Dart, no Flutter.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  riverpod: ^3.4.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
