/// Context assembly (#33): the one function that decides what a model
/// call carries — profile, memory, the task lists and the conversation —
/// and the hash that names what was sent. ADR 0011.
library;

export 'context/assemble.dart';
export 'context/hash.dart';
